version: '3'
services:
  web:
    build:
      context:
      dockerfile: Dockerfile
    ports:
      - "3002:3000"
    volumes:
      - /app/node_modules
      - .:/app
  test:
    build:
      context:
      dockerfile: Dockerfile
    volumes:
      - /app/node_modules
      - .:/app
    command: ["npm", "run", "test"]